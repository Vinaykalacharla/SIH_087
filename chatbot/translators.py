def translate_to_english(text: str) -> str:
    """
    Bypass translation and return the original text (English only mode).
    """
    return text

def translate_from_english(text: str, target_lang: str) -> str:
    """
    Bypass translation and return the original text (English only mode).
    """
    return text
